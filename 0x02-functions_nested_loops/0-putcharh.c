#include "main.h"

/**
* main - Entry point
 *
 * Description: A C  program will h
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char ch[] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};

for (int i = 0; i < 8; i++)
{
_putchar(ch[i]);
}
_putchar('\n');
return (0);
}
