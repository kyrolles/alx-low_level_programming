#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int ch = 0;
while (ch < 10)
{
printf("%d", ch);
ch++;
}
printf("\n");
return (0);
}
